const enum PLAYERS_REDUCER_ACTIONS {
    add_player,
    revive_player,
    add_status,
    reduce_status_duration,
    receive_damage,
    resource_change,
    play__attack_animation,
    REPLACE_ALL,
}

export default PLAYERS_REDUCER_ACTIONS;
